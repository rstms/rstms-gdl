# rstms-gdl

rstms-gdl is a minimal TLS client-certificate aware file downloader. It is
used to download startup config files in the installer ramdisk environment
where curl is not available and ftp does not support client certificates.
